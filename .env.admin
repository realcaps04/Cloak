# Legacy dual-mode only (`vite --mode admin` in this repo).
# Prefer the sibling folder: ../Cloak_Admin
# Do NOT put CLOAK_APP_ROLE=admin in the main .env — that breaks player Discord OAuth (19283 vs 19284).
CLOAK_APP_ROLE=admin
VITE_CLOAK_APP_ROLE=admin
